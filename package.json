{
  "name": "node-red-contrib-cognito-auth",
  "version": "0.0.1",
  "description": "authenticates against AWS Cognito to retrieve a JWT Token",
  "main": "index.js",
  "node-red": {
    "nodes": {
      "cognito-auth": "cognito-auth.js"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "npm@pro-md.de",
  "license": "ISC",
  "dependencies": {
    "amazon-cognito-identity-js": "^4.5.7",
    "jwt-decode": "^3.1.2"
  }
}
